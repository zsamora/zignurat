# General
LOG_LEVEL=debug
DEV_STAGE=development

# Identirat (IAM service)
IDENTIRAT_APP=identirat-app
IDENTIRAT_PORT=9000
IDENTIRAT_DB=identirat-db
IDENTIRAT_DB_PORT=5432
IDENTIRAT_DB_NAME=identirat
IDENTIRAT_DB_USER=postgres
IDENTIRAT_DB_PW=changeme

# JWT signing secret — use a long random string, never the value below
JWT_SECRET=changeme

# Session cookie signing secret — use a long random string, never the value below
SESSION_SECRET=changeme
